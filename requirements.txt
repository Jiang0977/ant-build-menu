# Core dependencies for Ant Build Menu
# Windows注册表操作
winreg2>=1.0.0  # 增强的注册表操作库

# 进程管理和执行
psutil>=5.9.0   # 系统和进程工具

# 配置文件处理
pyyaml>=6.0     # YAML配置文件支持

# XML解析 (用于解析build.xml)
lxml>=4.9.0     # 强大的XML处理库

# 日志管理
colorlog>=6.7.0 # 彩色日志输出

# 开发和测试依赖
pytest>=7.0.0   # 测试框架
pytest-cov>=4.0.0  # 测试覆盖率
black>=22.0.0   # 代码格式化
flake8>=5.0.0   # 代码检查

# 打包相关
setuptools>=65.0.0
wheel>=0.37.0
pyinstaller>=5.0.0  # 打包为exe 